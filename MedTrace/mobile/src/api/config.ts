// When testing on a physical phone via Expo Go, change this to your Mac's
// LAN IP (e.g. http://192.168.x.x:8080) because 'localhost' resolves to
// the phone itself, not your development machine.
export const API_BASE_URL = 'http://localhost:8080';

// Set to true to use hardcoded demo data (no backend required).
// Useful for portfolio screenshots and demos.
export const DEMO_MODE = true;

